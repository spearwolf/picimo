import SGScene from './s_g_scene'

import { SG_CANVAS } from './constants'

export default class SGCanvas extends SGScene {
  constructor () {
    super({ nodeType: SG_CANVAS })
  }

  // renderFrame () {
  // }
}
