
/**
 * Common component interface.
 */
export default class Component {
  // init (entity)
  // destroy (entity)
  // ?? create (config) { return new MyComponent(config) }
  // ?? update (config) { }
}
