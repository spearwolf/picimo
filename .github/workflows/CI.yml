name: CI

on: [push]

jobs:
  run_tests:
    name: Run tests
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@master
    - name: Install node package dependencies
      run: yarn install
    - name: Run tests
      run: yarn test

