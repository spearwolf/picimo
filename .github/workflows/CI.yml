name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@master
    - name: Install node package dependencies
      run: yarn install
    - name: Run tests
      run: yarn test
